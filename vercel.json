{
  "version": 2,
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "cd server && npm install && cd ../api && cd .. && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
